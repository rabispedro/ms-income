spring:
  application:
    name: api-gateway
  
  #cloud:
    #gateway:
      #mvc:
        #routes:
        #- id: worker-api:**
          #uri: lb://worker-api
          #predicates:
          #- Path=/worker-api/**

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  #instance:
    #hostname: localhost

server:
  port: 8765
  error:
    include-stacktrace: never
    include-message: always
    whitelabel:
      enabled: false